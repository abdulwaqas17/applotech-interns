/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
    domains: ["klbtheme.com","source.unsplash.com","html.pixelfit.agency","lume-demo1.myshopify.com"],
  },
};

export default nextConfig;

